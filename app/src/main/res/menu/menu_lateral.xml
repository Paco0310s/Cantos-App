<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_listaCantos"
            android:icon="@drawable/hogar"
            android:title="@string/app_name" />

        <item
            android:visible="false"
            android:id="@+id/nav_listaEsquemas"
            android:icon="@drawable/ic_lista"
            android:title="@string/esquemas" />

        <item
            android:visible="false"
            android:id="@+id/nav_afinador"
            android:icon="@drawable/ic_afinador"
            android:title="@string/afinador" />
    </group>

</menu>